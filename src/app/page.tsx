"use client";

import { useEffect, useMemo, useState } from "react";

type Day = { date:string; focusSec:number; earned:number; restSec:number; sessions:number };
type Pass = { id:number; minutes:number; cost:number; remainingSec:number };
type State = { goalMin:number; balance:number; today:Day; days:Day[]; passes:Pass[]; focusStartedAt:number|null; restStartedAt:number|null; restPassId:number|null; restRemainingAtStart:number; combo:number; streak:number; lastFocusDate:string|null };

const KEY="focus-wallet-pwa-v09";
const FOCUS_RATE=100, REST_RATE=200, OPTIONS=[5,10,20,30];
const pad=(n:number)=>String(n).padStart(2,"0");
const mmss=(s:number)=>`${pad(Math.floor(Math.max(0,s)/60))}:${pad(Math.max(0,s)%60)}`;
const money=(n:number)=>n.toLocaleString("ko-KR");
const dateKey=(d=new Date())=>{const x=new Date(d.getTime()-d.getTimezoneOffset()*60000);return x.toISOString().slice(0,10)};
function fresh():State{const today=dateKey(); const blank=(date:string):Day=>({date,focusSec:0,earned:0,restSec:0,sessions:0}); return {goalMin:60,balance:0,today:blank(today),days:[...Array(6)].map((_,i)=>{const d=new Date();d.setDate(d.getDate()-(6-i));return blank(dateKey(d))}).concat(blank(today)),passes:[],focusStartedAt:null,restStartedAt:null,restPassId:null,restRemainingAtStart:0,combo:0,streak:0,lastFocusDate:null};}
function loadState():State{try{const raw=localStorage.getItem(KEY); if(!raw)return fresh(); const s={...fresh(),...JSON.parse(raw)} as State; if(s.today.date!==dateKey()){const old=s.today; s.days=[...s.days.filter(d=>d.date!==old.date),old];s.today={date:dateKey(),focusSec:0,earned:0,restSec:0,sessions:0};s.combo=0;} return s;}catch{return fresh();}}
function save(s:State){localStorage.setItem(KEY,JSON.stringify(s));}

export default function Home(){
 const [s,setS]=useState<State|null>(null); const [now,setNow]=useState(Date.now()); const [installed,setInstalled]=useState(false); const [goalInput,setGoalInput]=useState(60);
 useEffect(()=>{const loaded=loadState(); setS(loaded); setGoalInput(loaded.goalMin); const t=setInterval(()=>setNow(Date.now()),1000); return()=>clearInterval(t)},[]);
 useEffect(()=>{if(s){save(s); if(s.restStartedAt && restLeft(s,now)<=0){finishRest(false);}}},[s?.balance,s?.today.focusSec,s?.today.restSec,s?.passes.length,s?.focusStartedAt,s?.restStartedAt]);
 useEffect(()=>{if("serviceWorker" in navigator) navigator.serviceWorker.register(`${process.env.NEXT_PUBLIC_BASE_PATH||""}/sw.js`).catch(()=>{});},[]);
 function restLeft(st:State,t=Date.now()){return Math.max(0,st.restRemainingAtStart-Math.floor((t-(st.restStartedAt||t))/1000));}
 function focusElapsed(){return s?.focusStartedAt?Math.max(0,Math.floor((now-s.focusStartedAt)/1000)):0;}
 function startFocus(){if(!s||s.restStartedAt)return;setS({...s,focusStartedAt:Date.now(),combo:s.combo});}
 function stopFocus(){if(!s?.focusStartedAt)return; const sec=focusElapsed(); if(sec<10){setS({...s,focusStartedAt:null});return;} const earned=Math.floor(sec/60)*FOCUS_RATE; const day={...s.today,focusSec:s.today.focusSec+sec,earned:s.today.earned+earned,sessions:s.today.sessions+1}; const key=dateKey(); let streak=s.streak; if(sec>=25*60){streak=s.lastFocusDate===dateKey(new Date(Date.now()-86400000))?streak+1:(s.lastFocusDate===key?streak:1);} const days=s.days.filter(d=>d.date!==key).concat({...day}); setS({...s,balance:s.balance+earned,today:day,days:days.slice(-7),focusStartedAt:null,combo:s.combo+1,streak,lastFocusDate:key});}
 function buyRest(minutes:number){if(!s||s.focusStartedAt||s.restStartedAt)return;const cost=minutes*REST_RATE;if(s.balance<cost)return;setS({...s,balance:s.balance-cost,passes:[...s.passes,{id:Date.now(),minutes,cost,remainingSec:minutes*60}]});}
 function startRest(id:number){if(!s||s.focusStartedAt||s.restStartedAt)return;const p=s.passes.find(x=>x.id===id);if(!p)return;setS({...s,restStartedAt:Date.now(),restPassId:id,restRemainingAtStart:p.remainingSec});}
 function finishRest(manual=true){if(!s?.restStartedAt||s.restPassId===null)return;const used=Math.max(0,s.restRemainingAtStart-(Math.floor((Date.now()-s.restStartedAt)/1000)));const p=s.passes.find(x=>x.id===s.restPassId);if(!p)return;const remaining=Math.max(0,p.remainingSec-used);const day={...s.today,restSec:s.today.restSec+used};const passes=remaining>0?s.passes.map(x=>x.id===p.id?{...x,remainingSec:remaining}:x).filter(x=>x.remainingSec>0):s.passes.filter(x=>x.id!==p.id);setS({...s,today:day,passes,restStartedAt:null,restPassId:null,restRemainingAtStart:0});}
 function setGoal(){if(!s)return; const goal=Math.max(1,Math.min(1440,Math.floor(Number(goalInput)||60))); setS({...s,goalMin:goal});} function reset(){if(confirm("기록과 잔액을 모두 초기화할까요?")){const x=fresh();setS(x);setGoalInput(x.goalMin);save(x)}}
 if(!s)return <main style={styles.center}>Focus Wallet 불러오는 중…</main>;
 const focusSec=focusElapsed(); const restSec=s.restStartedAt?restLeft(s,now):0; const weekly=s.days.reduce((a,d)=>({focusSec:a.focusSec+d.focusSec,earned:a.earned+d.earned,restSec:a.restSec+d.restSec,sessions:a.sessions+d.sessions}),{focusSec:0,earned:0,restSec:0,sessions:0}); const todayMin=Math.floor(s.today.focusSec/60); const preview=Math.floor(focusSec/60)*FOCUS_RATE; const max=Math.max(...s.days.map(d=>d.focusSec),60); const active=!!s.focusStartedAt||!!s.restStartedAt;
 const goalMin=s.goalMin; const missions=[{title:"25분 집중",desc:"한 번에 25분 이상 집중",current:Math.min(25,Math.floor(s.today.focusSec/60)),target:25,reward:500},{title:"3세션 달성",desc:"오늘 집중 세션 3회 만들기",current:Math.min(3,s.today.sessions),target:3,reward:700},{title:`${goalMin}분 목표`,desc:"오늘 설정한 집중 목표 달성",current:Math.min(goalMin,todayMin),target:goalMin,reward:1000}];
 return <main className="fw-page" style={styles.page}><style>{`@media (max-width: 700px){.fw-page{padding:14px 12px 40px!important}.fw-header{align-items:flex-start!important}.fw-logo{font-size:20px!important}.fw-grid{grid-template-columns:1fr!important}.fw-card{padding:17px!important;border-radius:18px!important}.fw-money{font-size:44px!important}.fw-timer{font-size:64px!important}.fw-shop{grid-template-columns:repeat(2,1fr)!important}.fw-stats,.fw-weekstats{grid-template-columns:repeat(2,1fr)!important}.fw-missions{grid-template-columns:1fr!important}.fw-gamtop{flex-direction:column;align-items:flex-start!important}.fw-streak{text-align:left!important;width:100%;box-sizing:border-box}.fw-chart{height:160px!important}.fw-barcol{height:150px!important}.fw-goal-editor{flex-wrap:wrap!important}}`}</style>
  <header className="fw-header" style={styles.header}><div><div className="fw-logo" style={styles.logo}>🎯 Focus Wallet</div><div style={styles.sub}>집중해서 벌고, 원하는 휴식을 사세요.</div></div><button onClick={reset} style={styles.link}>기록 초기화</button></header>
  <section className="fw-grid" style={styles.grid}>
   <div className="fw-card" style={{...styles.card,...styles.balance}}><div style={styles.label}>현재 보유금</div><div className="fw-money" style={styles.money}>₩{money(s.balance)}</div><div style={styles.usable}>휴식 가능 <b>{Math.floor(s.passes.reduce((a,p)=>a+p.remainingSec,0)/60)}분</b></div><div style={styles.goalRow}><span>🎯 오늘 목표 {goalMin}분</span><b>{todayMin}분</b></div><div style={styles.progress}><div style={{...styles.fill,width:`${Math.min(100,todayMin/goalMin*100)}%`}}/></div><div style={styles.goalEditor}><input aria-label="오늘 목표 분" type="number" min="1" max="1440" value={goalInput} onChange={e=>setGoalInput(Number(e.target.value))} style={styles.goalInput}/><span>분</span><button onClick={setGoal} style={styles.secondary}>목표 저장</button></div><div className="fw-stats" style={styles.stats}><div><span style={styles.label}>오늘 번 돈</span><b>₩{money(s.today.earned)}</b></div><div><span style={styles.label}>오늘 휴식</span><b>{Math.floor(s.today.restSec/60)}분</b></div><div><span style={styles.label}>세션</span><b>{s.today.sessions}회</b></div></div></div>
   <div className="fw-card" style={{...styles.card,...styles.focus}}><div style={styles.label}>{s.restStartedAt?"🌿 휴식 중":"⏱️ 집중 · 1분 = ₩100"}</div><div className="fw-timer" style={styles.timer}>{s.focusStartedAt?mmss(focusSec):s.restStartedAt?mmss(restSec):"00:00"}</div><div style={styles.earn}>{s.focusStartedAt?`이번 세션 예상 ₩${money(preview)}`:s.restStartedAt?"잠깐 쉬고 다시 집중해보세요":"집중한 만큼 돈이 쌓입니다"}</div><div style={styles.actions}>{s.focusStartedAt?<button onClick={stopFocus} style={styles.danger}>■ 집중 종료</button>:s.restStartedAt?<button onClick={()=>finishRest(true)} style={styles.secondary}>휴식 종료</button>:<button onClick={startFocus} style={styles.primary}>▶ 집중 시작</button>}</div><div style={styles.note}>데이터는 이 기기의 브라우저에만 저장됩니다.</div></div>
  </section>
  <section className="fw-card" style={styles.card}><h2 style={styles.title}>🌿 휴식 상점</h2><div className="fw-shop" style={styles.shop}>{OPTIONS.map(min=><div key={min} style={styles.shopItem}><div style={styles.shopEmoji}>☕</div><b>{min}분</b><span>₩{money(min*REST_RATE)}</span><button disabled={active||s.balance<min*REST_RATE} onClick={()=>buyRest(min)} style={styles.secondary}>구매</button></div>)}</div><div style={styles.note}>환율: ₩200 = 휴식 1분 · 하루 사용 한도 없이 원하는 만큼 사용할 수 있어요. 구매한 휴식권의 남은 시간은 다음날에도 그대로 유지됩니다.</div></section>
  <section className="fw-card" style={styles.card}><h2 style={styles.title}>🎟️ 보유 휴식권</h2>{!s.passes.length?<div style={styles.empty}>아직 휴식권이 없습니다. 집중해서 코인을 모아보세요.</div>:<div style={styles.passList}>{s.passes.map(p=><div key={p.id} style={styles.pass}><div><b>{p.minutes}분 휴식권</b><div style={styles.sub}>₩{money(p.cost)} · 남은 {Math.ceil(p.remainingSec/60)}분</div></div><button disabled={active} onClick={()=>startRest(p.id)} style={styles.primarySmall}>▶ 사용</button></div>)}</div>}</section>
  <section style={styles.card}><div className="fw-gamtop" style={styles.gamTop}><div><div style={styles.label}>🔥 오늘의 집중 콤보</div><div style={styles.comboBig}>{s.combo} <span>COMBO</span></div><div style={styles.sub}>세션 {s.today.sessions}회</div></div><div className="fw-streak" style={styles.streakBox}><div style={styles.streakNum}>🏆 {s.streak}</div><div style={styles.label}>25분 이상 집중 연속 일수</div></div></div><div style={styles.missionTitle}>🎯 오늘의 미션</div><div className="fw-missions" style={styles.missionGrid}>{missions.map(m=>{const pct=Math.min(100,m.current/m.target*100);return <div key={m.title} style={styles.mission}><div style={styles.missionHead}><b>{m.title}</b><span>{m.current>=m.target?"✅":"진행"}</span></div><div style={styles.missionDesc}>{m.desc}</div><div style={styles.missionBar}><div style={{...styles.missionFill,width:`${pct}%`}}/></div><div style={styles.missionFoot}><span>{m.current}/{m.target}</span><b>+₩{money(m.reward)}</b></div></div>})}</div><div style={styles.note}>미션 보상은 현재 버전에서 진행도 표시용입니다.</div></section>
  <section style={styles.card}><div style={styles.sectionHeader}><h2 style={styles.title}>📊 최근 7일 집중 기록</h2><span style={styles.weekTotal}>{Math.floor(weekly.focusSec/60)}분</span></div><div className="fw-chart" style={styles.chart}>{s.days.map(day=>{const h=Math.max(4,Math.round(day.focusSec/max*150));const dow=new Date(day.date+"T00:00:00").toLocaleDateString("ko-KR",{weekday:"short"}).replace("요일","");return <div key={day.date} style={styles.barCol}><div style={{...styles.bar,height:h}}/><span style={styles.barLabel}>{dow}</span><span style={styles.barValue}>{Math.floor(day.focusSec/60)}</span></div>})}</div><div className="fw-weekstats" style={styles.weekStats}><div><span style={styles.label}>집중</span><b>{Math.floor(weekly.focusSec/60)}분</b></div><div><span style={styles.label}>획득</span><b>₩{money(weekly.earned)}</b></div><div><span style={styles.label}>휴식</span><b>{Math.floor(weekly.restSec/60)}분</b></div><div><span style={styles.label}>세션</span><b>{weekly.sessions}회</b></div></div></section>
  <section className="fw-card" style={styles.card}><h2 style={styles.title}>📱 앱으로 사용하기</h2><p style={styles.sub}>휴대폰 브라우저에서 <b>홈 화면에 추가</b>하면 앱 아이콘으로 바로 실행할 수 있어요. 설치가 지원되는 브라우저에서는 아래 버튼이 나타납니다.</p>{!installed&&<button onClick={()=>setInstalled(true)} style={styles.secondary}>홈 화면에 추가 안내 보기</button>} {installed&&<div style={styles.installBox}>브라우저 메뉴에서 <b>“홈 화면에 추가”</b> 또는 <b>“앱 설치”</b>를 선택하세요. iPhone은 Safari의 공유 메뉴 → 홈 화면에 추가를 사용하면 됩니다.</div>}</section>
 </main>;
}
const styles:Record<string,React.CSSProperties>={page:{maxWidth:1000,margin:"0 auto",padding:"24px 16px 60px",background:"#f6f7fb",minHeight:"100vh",fontFamily:"system-ui,sans-serif",color:"#202533"},center:{minHeight:"100vh",display:"grid",placeItems:"center",fontFamily:"system-ui,sans-serif"},header:{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:18},logo:{fontSize:23,fontWeight:950},sub:{color:"#737b8c",lineHeight:1.55},link:{border:0,background:"transparent",color:"#6c63ff",fontWeight:900,cursor:"pointer"},grid:{display:"grid",gridTemplateColumns:"1.08fr .92fr",gap:16},card:{background:"#fff",border:"1px solid #e7eaf0",borderRadius:22,padding:22,boxShadow:"0 14px 35px rgba(39,46,67,.07)",marginBottom:16},balance:{background:"linear-gradient(135deg,#f0efff,#fff)"},focus:{textAlign:"center"},label:{fontSize:13,color:"#737b8c",fontWeight:850},money:{fontSize:60,fontWeight:950,letterSpacing:"-.06em"},usable:{color:"#6c63ff",fontWeight:900},goalRow:{display:"flex",justifyContent:"space-between",fontSize:13,fontWeight:850,marginTop:25},progress:{height:11,background:"#eceef4",borderRadius:99,overflow:"hidden",marginTop:8},fill:{height:"100%",background:"linear-gradient(90deg,#6c63ff,#8b84ff)",borderRadius:99},stats:{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:10,marginTop:17},timer:{fontSize:86,fontWeight:950,letterSpacing:"-.07em",fontVariantNumeric:"tabular-nums",margin:"8px 0"},earn:{color:"#32a970",fontWeight:900,minHeight:25},actions:{display:"flex",justifyContent:"center",marginTop:15},primary:{border:0,borderRadius:14,padding:"13px 22px",background:"#6c63ff",color:"#fff",fontWeight:900,cursor:"pointer"},danger:{border:0,borderRadius:14,padding:"13px 22px",background:"#fff0f1",color:"#b94f5b",fontWeight:900,cursor:"pointer",border:"1px solid #ffd3d8"},secondary:{border:0,borderRadius:12,padding:"10px 14px",background:"#eef0f6",color:"#4c5361",fontWeight:900,cursor:"pointer"},note:{fontSize:12,color:"#99a0ad",marginTop:12,lineHeight:1.55},title:{margin:"0 0 14px",fontSize:19},shop:{display:"grid",gridTemplateColumns:"repeat(4,1fr)",gap:10},shopItem:{border:"1px solid #e7eaf0",borderRadius:17,padding:14,display:"grid",gap:7},shopEmoji:{fontSize:27},passList:{display:"grid",gap:9},pass:{display:"flex",justifyContent:"space-between",alignItems:"center",gap:10,padding:13,border:"1px solid #e7eaf0",borderRadius:15},primarySmall:{border:0,borderRadius:12,padding:"9px 12px",background:"#6c63ff",color:"#fff",fontWeight:900,cursor:"pointer"},empty:{padding:16,borderRadius:15,background:"#fafbfc",color:"#7b8391"},sectionHeader:{display:"flex",justifyContent:"space-between",alignItems:"center"},weekTotal:{fontSize:18,fontWeight:950,color:"#6c63ff"},chart:{display:"flex",alignItems:"flex-end",justifyContent:"space-between",height:190,gap:8,padding:"8px 4px 0",borderBottom:"1px solid #eceef4"},barCol:{height:180,display:"flex",flexDirection:"column",alignItems:"center",justifyContent:"flex-end",gap:5,flex:1},bar:{width:"70%",maxWidth:30,minHeight:4,borderRadius:"8px 8px 3px 3px",background:"#6c63ff"},barLabel:{fontSize:11,color:"#8a92a0"},barValue:{fontSize:10,color:"#6b7280"},weekStats:{display:"grid",gridTemplateColumns:"repeat(4,1fr)",gap:10,marginTop:14},gamTop:{display:"flex",justifyContent:"space-between",gap:16,alignItems:"center",padding:"4px 0 18px"},comboBig:{fontSize:48,fontWeight:950,letterSpacing:"-.05em",color:"#6c63ff",lineHeight:1},streakBox:{textAlign:"right",padding:"12px 14px",background:"#fff9e8",borderRadius:16,border:"1px solid #f4e5b1"},streakNum:{fontSize:25,fontWeight:950,marginBottom:4},missionTitle:{fontSize:17,fontWeight:950,margin:"4px 0 10px"},missionGrid:{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:10},mission:{border:"1px solid #e7eaf0",borderRadius:16,padding:14,background:"#fafbff"},missionHead:{display:"flex",justifyContent:"space-between",gap:8,fontSize:14},missionDesc:{fontSize:12,color:"#737b8c",margin:"7px 0 10px",lineHeight:1.45},missionBar:{height:8,borderRadius:99,background:"#e9ebf1",overflow:"hidden"},missionFill:{height:"100%",borderRadius:99,background:"#6c63ff"},missionFoot:{display:"flex",justifyContent:"space-between",fontSize:12,color:"#737b8c",marginTop:7},goalEditor:{display:"flex",alignItems:"center",gap:7,marginTop:10},goalInput:{width:78,padding:"8px 9px",border:"1px solid #dfe3eb",borderRadius:10,fontSize:14,fontWeight:800,boxSizing:"border-box"},installBox:{marginTop:12,padding:14,borderRadius:14,background:"#f5f4ff",color:"#56516f",lineHeight:1.6}};
